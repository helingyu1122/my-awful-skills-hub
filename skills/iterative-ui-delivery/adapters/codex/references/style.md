# 风格

## 风格原则

- 统一优先于炫技：先保证整站一致，再做局部亮点
- 延续已有视觉语言：颜色、圆角、边框、阴影、状态统一
- 禁止“系统默认控件穿帮”：原生控件与页面风格不一致时，改为自定义组件

## 控件一致性要求

- 按钮：主次按钮层级明确，hover/focus/disabled 状态完整
- 下拉：触发器与弹层统一风格，不使用系统默认弹出样式
- 表单：输入框、下拉、文本域边框与焦点态保持一套 token
- 表格：表头、行 hover、操作列风格一致

## 风格变更策略

- 每轮最多引入一类新视觉模式
- 新模式先在一个页面试点，通过后再推广
